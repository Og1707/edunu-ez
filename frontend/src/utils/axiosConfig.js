/**
 * @deprecated Usar import api from '../services/api' directamente.
 * Este archivo se mantiene temporalmente para compatibilidad con componentes
 * que aún lo importan. Redirige a la instancia única de api.js.
 */
import api from '../services/api';

export default api;
